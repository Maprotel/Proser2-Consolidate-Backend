{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "proserConsolidate": {
    "host": "${INVENTORY_DB_HOST}",
    "port": 3306,
    "url": "",
    "database": "${PROSER_INVENTORY_DATABASE}",
    "password": "${INVENTORY_DB_PASSWORD_WRITE}",
    "name": "proserConsolidate",
    "user": "${INVENTORY_DB_USER_WRITE}",
    "connector": "mysql",
    "connectTimeout": 30000,
    "acquireTimeout": 20000
  }
}