{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "proserConsolidate": {
    "host": "${INVENTORY_DB_HOST}",
    "port": 3306,
    "url": "",
    "database": "${CONSOLIDATE_DATABASE}",
    "password": "${CONSOLIDATE_DB_PASSWORD}",
    "name": "proserConsolidate",
    "user": "${CONSOLIDATE_DB_USER}",
    "connector": "mysql",
    "connectTimeout": 30000,
    "acquireTimeout": 20000
  }
}